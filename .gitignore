# brief.md is intentionally NOT ignored: the workflow commits it, and
# publishing it is the point of the repo. Ignoring it makes `git add brief.md`
# exit non-zero, which aborts the commit step under `bash -e`.
cache/
__pycache__/
*.pyc
venv/
.venv/
.DS_Store
